# Optional build-time backend origin, without a trailing /api.
# Azure production: leave empty; Spring Boot serves the frontend and /api together.
# Development forwards /api to http://localhost:8080 using the Vite proxy.
VITE_API_BASE_URL=
